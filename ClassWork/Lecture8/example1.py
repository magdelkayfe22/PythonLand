# If-Else Statement (Decision Structure)

num1 = eval( input("Enter the first number: " ) )
num2 = eval( input("Enter the second number: ") )

if num1 > num2:
    largerValue = num1
else:
    largerValue = num2

print("The larger value is:", str( largerValue ) + ".")